import { types } from '../../types';

/**@typedef {import('../Grid/Grid.types').props} gridProps */

/**
 * @typedef {object} auxProps
 * @property {types.productTypes.products} products
 */

/**
 * @typedef {auxProps & gridProps} ProductGridProps
 */

export {};
